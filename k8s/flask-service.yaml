apiVersion: v1
kind: Service

metadata:
  name: flask-service
  namespace: flask-webapp-demo

spec:
  type: NodePort

  selector:
    app: flask-webapp

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

